# reward_model

Reward model training pipeline using a Bradley-Terry preference loss.

## Structure

- `configs/default.yaml` — hyperparameters
- `src/reward_model/` — model, loss, dataset, trainer, evaluator, utils
- `scripts/` — train, evaluate, inference entry points
- `tests/` — unit tests
