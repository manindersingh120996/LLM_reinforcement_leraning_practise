"""Training loop placeholder for reward model."""

def train_one_epoch(model, dataloader, optimizer):
    model.train()
    for batch in dataloader:
        inputs = batch["input"]
        # placeholder training step
        loss = 0.0
    return
